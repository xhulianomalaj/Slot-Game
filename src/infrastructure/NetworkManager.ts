// @deprecated — moved to '@/infrastructure/network'.
// Kept as a re-export so existing imports keep working. Update your imports
// to `@/infrastructure/network` and delete this file when convenient.

export { MockNetworkManager } from './network/MockNetworkManager';
export type { NetworkManager } from './network/types';
